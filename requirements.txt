# Faceless Shorts Factory - Python dependencies
# Everything here is free and installs on a GitHub Actions ubuntu-latest runner.

# HTTP client (Gemini REST API, RSS fetching, Pexels)
requests>=2.32.0,<3.0.0

# Parses the RSS/Atom news feeds we source stories from
feedparser>=6.0.11,<7.0.0

# Microsoft Edge's online text-to-speech. Free, no API key, and it returns
# word-level timings which we turn into burned-in captions.
edge-tts>=6.1.0

# Google auth + YouTube Data API v3 client (resumable upload)
google-api-python-client>=2.140.0,<3.0.0
google-auth>=2.35.0,<3.0.0
google-auth-oauthlib>=1.2.0,<2.0.0
google-auth-httplib2>=0.2.0,<1.0.0
